# Сертификаттар мен видеоларды қалай қосуға болады

## 📜 Сертификаттарды қосу

### 1-қадам: Папканы құру
`public` папкасында `certificates` папкасын құрыңыз:
```
public/
  └── certificates/
```

### 2-қадам: PNG фото және PDF файлдарын қосу

Әр сертификат үшін **екі файл** қажет:
1. **PNG фото** - сайтта көрінетін сурет
2. **PDF файл** - басылғанда жүктелетін файл

Файлдарды `public/certificates/` папкасына қойыңыз:
- `cert1.png` + `cert1.pdf`
- `cert2.png` + `cert2.pdf`
- `cert3.png` + `cert3.pdf`
- `cert4.png` + `cert4.pdf`

**Қалай жұмыс істейді:**
- Сайтта PNG фото көрінеді
- Фотоға басқанда PDF файл жүктеледі (скачивание)

**Ескерту:** Егер сіз басқа атаулар пайдалансаңыз, `src/app/[locale]/nashi-uslugi/page.tsx` файлындағы `certificates` массивіндегі `image` және `pdf` жолдарын өзгертіңіз.

### 3-қадам: Көбірек сертификат қосу
Егер 4-тен көп сертификат қосу керек болса:
1. `page.tsx` файлындағы `certificates` массивіне жаңа элемент қосыңыз:
```typescript
{ 
  id: 5, 
  image: "/certificates/cert5.png",  // PNG фото
  pdf: "/certificates/cert5.pdf",    // PDF файл
  title: t("certificates.item5.title"), 
  description: t("certificates.item5.description") 
}
```
2. `messages/kz.json`, `messages/ru.json`, `messages/en.json` файлдарына `certificates.item5` переводын қосыңыз.
3. `public/certificates/` папкасына `cert5.png` және `cert5.pdf` файлдарын қойыңыз.

---

## 🎥 Видеоларды қосу

### Вариант 1: YouTube видеолар

1. YouTube-тағы видеоны ашыңыз
2. Видео ID-ін алыңыз (мысалы: `https://www.youtube.com/watch?v=dQw4w9WgXcQ` → ID: `dQw4w9WgXcQ`)
3. `page.tsx` файлындағы `videos` массивінде `url`-ді өзгертіңіз:
```typescript
{
  id: 1,
  type: "youtube",
  url: "https://www.youtube.com/embed/dQw4w9WgXcQ", // VIDEO_ID_1 орнына нақты ID
  title: t("videos.item1.title"),
  description: t("videos.item1.description")
}
```

### Вариант 2: Локальді видеолар (компьютердегі файлдар)

1. `public` папкасында `videos` папкасын құрыңыз:
```
public/
  └── videos/
```

2. Видео файлдарын қойыңыз:
- `video1.mp4`
- `video2.mp4`
- `video3.mp4`

3. `page.tsx` файлында `type: "local"` және `localPath` қолданыңыз:
```typescript
{
  id: 1,
  type: "local",
  localPath: "/videos/video1.mp4",
  title: t("videos.item1.title"),
  description: t("videos.item1.description")
}
```

**Ескерту:** Видео файлдары үлкен болуы мүмкін, сондықтан оларды оңтайландыру ұсынылады.

---

## 📝 Мысал

### Сертификаттар үшін:
```
public/
  └── certificates/
      ├── cert1.png    (сайтта көрінетін фото)
      ├── cert1.pdf    (жүктелетін PDF)
      ├── cert2.png
      ├── cert2.pdf
      ├── cert3.png
      ├── cert3.pdf
      ├── cert4.png
      └── cert4.pdf
```

### Видеолар үшін (локальді):
```
public/
  └── videos/
      ├── video1.mp4
      ├── video2.mp4
      └── video3.mp4
```

### Видеолар үшін (YouTube):
Кодта тек URL-ді өзгертіңіз, файл қосу қажет емес.

---

## ⚠️ Ескертулер

1. **Файл атаулары:** Файл атауларында бос орын болмауы керек. Мысалы: `cert 1.png` емес, `cert1.png`
2. **Файл форматы:** 
   - Сертификат фото: `.png` (ұсынылады), `.jpg`, `.jpeg`, `.webp`
   - Сертификат PDF: `.pdf`
   - Видеолар: `.mp4`, `.webm`, `.ogg`
3. **Файл өлшемі:** Үлкен файлдар сайтты баяулатады, сондықтан оларды сығу ұсынылады
4. **Өзгерістер:** Файлдарды қосқаннан кейін серверді қайта іске қосыңыз
5. **PDF жүктеу:** Сертификат фотосына басқанда PDF файл автоматты түрде жүктеледі

